import styled from 'styled-components';

interface AuthModeType {}

const AuthModeBtn = ({ children, onClick }) => {
	return <div></div>;
};

export default AuthModeBtn;

const AuthModeBtnContainer = styled.div`
	display: flex;
	justify-content: center;
	align-items: center;
`;
