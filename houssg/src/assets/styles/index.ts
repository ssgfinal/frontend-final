import { color, basicTheme } from './theme';
import GlobalStyle from './GlobalStyle';
//Auth관련
import { AuthTitle, AuthContainer } from './StyledComponents';
//Manage관련
import { HouseInfoContainer, InfoText, SubInfoAligner, InfoWrapper, HouseTabContainer } from './StyledComponents';

export { color, basicTheme, GlobalStyle, AuthTitle, AuthContainer, HouseInfoContainer, InfoText, SubInfoAligner, InfoWrapper, HouseTabContainer };
